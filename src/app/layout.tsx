import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CGPA Calculator - Nigerian University Grade System",
  description: "Professional CGPA calculator for Nigerian university students. Calculate your GPA, CGPA, and track your academic progress with support for multiple semesters.",
  keywords: "CGPA, GPA, calculator, Nigerian university, grades, academic, university calculator",
  authors: [{ name: "CGPA Calculator" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
